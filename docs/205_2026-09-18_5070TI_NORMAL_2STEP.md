# 5070 Ti 补部署：局部重绘法线参考版 / 2 步

2026-09-18，用户确认节点恢复后补齐此前离线期间的部署。

## 范围

仅同步 `worker-5070ti-01` 的 `modelview-inpaint`。沿用已上线的
`2026.09.18-refcontrol-normal-2step-r1`，不再创建工作流版本，不修改其他节点或单视图流程。
保持四图输入、默认提示词、两套 LoRA 强度 1.0 / 0.8、simple 2 步和单张最终图输出。
API、前端接口与工作流图结构均未变。

节点先切 DRAINING，确认数据库任务、资产 Worker 任务和 ComfyUI 队列为空后同步文件。
新增 LoRA 安装至 `/opt/modelviewcreator/model/lora/flux-kelin/flux2_klein_9b_refcontrol_normal.safetensors`。
UI 在原有挂载文件上原位更新，未重启 ComfyUI、GPU 或控制服务。原 UI 保存在该节点
`/opt/modelviewcreator/backups/normal-2step-20260918/previous.ui.json`。

## 校验与验收

- 容器可见 UI SHA-256：`81c7f6b2f427799ee0f185dbf817a29e5d6df5f618de8c31fe6ccf5aabbe0bdf`，与其余三节点一致。
- 新 LoRA SHA-256：`baa97f297d048330850f0cb8063392678927a34282de961760693f6897983fd2`，与提供原件一致。
- 已有 li3d4500 LoRA SHA-256：`80c1e864ebe62c2a5e975385e0b70d2d7c7fb679cc39cb8f458a12c46cb3343e`。
- API 模板摘要：`65b0c6d0a3445d8318c411fd421b1721e33991494bd5b63d770bbcf29b67da7f`。
- 验收使用同一组用户提供的填白效果图、参考图、外扩蒙版和法线图；固定测试 seed=4500。
  校验 #15 steps=2、#79→#77→#74 的法线连线，以及最终 #29 仅返回一张 2048×2048 PNG。

验收已通过，prompt_id `50a5e687-7d3f-4f9d-b307-c297698fe1d3`，提交至取回结果 85.24 秒。
结果 SHA-256：`8a51bcf155fc8e8df466d1b673a8f106667bdf0dc082742e62c2baf9bed74df8`。
节点已恢复 ACTIVE/ONLINE，新版工作流兼容性为 true。

真实生成通过后，更新仅此节点、此工作流的 16GB 验收档案，绑定当前 GPU UUID、新版
模板哈希、实际生成证据哈希及本次读取的运行镜像/启动参数等 runtime.json 摘要。
原全局声明仍为 24000 MiB；没有放宽其他 GPU 的显存要求。
重新计算兼容性为 true 后才恢复 ACTIVE。其余工作流的资源验收档案保持原样。

运行配置保持 `--disable-dynamic-vram --lowvram --reserve-vram 3.0 --cpu-vae
--disable-async-offload --disable-pinned-memory`。注意系统目前对 runtime_profile_sha256
只验证摘要格式，不会自动核对未来配置漂移；后续若改运行配置仍须重新验收。

证据目录：`/srv/gpu-control/jobs/deployment-modelview-5070-normal-20260918/`，
含 previous.json、runtime.json、worker-5070ti-01.json/PNG、release.json。
发布脚本归档：`scripts/modelview_5070_normal_20260918/`。
前端 MD 的在线节点说明已更新，输入接口不用再对接。

验收仅证明本次真实样例在此硬件配置成功，不等同于任意分辨率或素材的质量/显存承诺。
旧文件、模型和档案均保留；没有上传 Git/LFS。
