# 工作流注册目录

这里只保存 ComfyUI **Export Workflow (API)** 导出的模板、参数 schema 和 manifest，不保存 UI 普通保存格式。生产工作流尚未提供，因此 `examples/` 保持为空；导入步骤见 [`docs/10_WORKFLOW_ONBOARDING.md`](../docs/10_WORKFLOW_ONBOARDING.md)。

